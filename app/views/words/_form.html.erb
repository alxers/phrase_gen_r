<%= form_for @word, html: { class: 'words-form' } do |f| %>
  <%= f.label :word %>
  <%= f.text_field :word %>
  <%= f.label :kind %>
  <%= f.select :kind, [:noun, :adjective] %>
  <%= f.submit 'Save' %>
<% end %>
